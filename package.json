{
  "name": "ai-agent-chiranjeeb-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
